<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aofei.sys.mapper.DeptMapper">


    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
          a.ID_DEPT AS deptId
        , a.ID_DEPT_PARENT AS parentId
        , a.NAME AS name
        , a.ORDER_NUM AS orderNum
        , a.CREATE_USER AS createUser
        , a.UPDATE_USER AS updateUser
        , a.CREATE_TIME AS createTime
        , a.UPDATE_TIME AS updateTime
        , a.DEL_FLAG AS delFlag
    </sql>

    <select id="findList"  resultType="com.aofei.sys.entity.Dept">
        select
        <include refid="Base_Column_List"/>
        from KDI_SYS_DEPT a
        <where>
            a.DEL_FLAG = 0
            <if test="parentId !=null ">
                AND  a.ID_DEPT_PARENT  = #{parentId}
            </if>
            <if test="name != null and name != ''">
                AND a.NAME LIKE '%#{name}%'
            </if>
        </where>
    </select>


    <update id="deleteById">
        update KDI_SYS_DEPT a set a.DEL_FLAG = 1  where a.ID_DEPT = #{id}
    </update>

</mapper>
